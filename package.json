{
  "name": "steal",
  "version": "0.0.5",
  "private": true,
  "type": "module",
  "description": "Steal, a Chrome MV3 extension. Point at an element and copy it to your clipboard.",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && vite build --mode content && vite build --mode background",
    "dev": "npm run clean && (vite build --watch --mode content & vite build --watch --mode background & wait)",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "gen-icons": "python3 tools/gen-icons.py"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "jsdom": "^27.0.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-static-copy": "^2.1.0",
    "vitest": "^2.1.4"
  }
}
